/*

Di seguito una breve spiegazione del progetto creato da Dart.

File per git. 
Questi file non servono a Dart ma solo per il versionamento di git.
Se non usate git potete cancellarli.
File: 
    .gitignore
    CHANGELOG.md
    README.md

File per Dart.
Questi file sono i pacchetti della libreria di dart (dipendenze).
Se ne possono aggiungere altri da "pub.dev".
File:
    .dart_tool
    .packages
    pubspec.lock
    pubspec.yaml

File per lins (linting).
Regole controllate quando scriviamo il codice (es nome variabili in camelCase).
File:
    analysus_options.yaml

Cartella bin.
Contiene il codice vero e proprio che verrà eseguito.
Contiene il main.

Cartella lib.
Contiene sempre codice ma di librerie interne o esterne.

Cartella test.
Come dice il nome stesso contiene il codice per testare in modo automatizzato l'applicazione.



*/